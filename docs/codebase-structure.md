# 프로젝트 폴더 구조

> **작성일**: 2025-01-04
> **Next.js 버전**: 15.5.2 (App Router)
> **표준**: Next.js 2025 Community Best Practices

## 📋 전체 구조 개요

이 프로젝트는 **Next.js 15 App Router**를 기반으로 하며, 2025년 커뮤니티 표준을 따릅니다.

### 핵심 원칙

1. **Colocation**: 컴포넌트를 사용하는 곳 가까이에 배치
2. **Private Folders (`_components/`)**: 특정 라우트에서만 사용하는 컴포넌트
3. **Features 기반**: 도메인별로 컴포넌트 그룹화
4. **평탄한 구조**: 과도한 중첩 방지

---

## 🗂️ 폴더 구조

```
batorment-front/
├── data/                           # JSON 데이터 파일
│   ├── raids.json                  # 레이드 정보
│   ├── students.json               # 학생 정보
│   └── links/                      # 동적 링크 데이터
│
├── public/                         # 정적 파일
│   └── favicon.ico
│
├── src/
│   ├── app/                        # Next.js App Router
│   │   ├── layout.tsx              # 루트 레이아웃
│   │   ├── page.tsx                # 홈페이지 (총력전/대결전 검색)
│   │   ├── error.tsx               # 전역 에러 페이지
│   │   │
│   │   ├── calculator/
│   │   │   └── score/
│   │   │       └── page.tsx        # 점수 계산기
│   │   │
│   │   └── video-analysis/
│   │       ├── page.tsx            # 영상 분석 목록
│   │       ├── _components/        # Private: 이 경로에서만 사용
│   │       │   ├── video-list.tsx
│   │       │   └── video-detail.tsx
│   │       └── [id]/
│   │           ├── page.tsx        # 영상 상세
│   │           ├── _components/    # Private: [id]에서만 사용
│   │           │   └── editable-analysis-result.tsx
│   │           └── edit/
│   │               └── page.tsx    # 영상 편집
│   │
│   ├── components/                 # 전역 공유 컴포넌트
│   │   ├── ui/                     # shadcn/ui 기본 컴포넌트
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   ├── input.tsx
│   │   │   └── custom/             # 커스텀 UI 컴포넌트
│   │   │       ├── hybrid-tooltip.tsx
│   │   │       └── single-select.tsx
│   │   │
│   │   ├── layout/                 # 레이아웃 컴포넌트
│   │   │   ├── app-sidebar.tsx     # 사이드바
│   │   │   ├── dark-mode-toggle.tsx # 다크모드 토글
│   │   │   └── sidebar-trigger.tsx  # 사이드바 트리거
│   │   │
│   │   ├── common/                 # 공통 컴포넌트
│   │   │   ├── loading.tsx         # 로딩 스피너
│   │   │   ├── error-page.tsx      # 에러 페이지
│   │   │   ├── card-wrapper.tsx    # 카드 래퍼
│   │   │   ├── info-fab.tsx        # 정보 FAB
│   │   │   ├── coffee.tsx          # 후원 버튼
│   │   │   └── normal-announce.tsx # 공지사항
│   │   │
│   │   ├── shared/                 # 재사용 가능한 복합 컴포넌트
│   │   │   ├── cascader.tsx        # 계층형 선택
│   │   │   ├── multi-select.tsx    # 다중 선택
│   │   │   └── pagination.tsx      # 페이지네이션
│   │   │
│   │   └── features/               # 도메인별 컴포넌트
│   │       ├── raid/               # 레이드 관련
│   │       │   ├── raid-search.tsx      # 파티 검색
│   │       │   ├── raid-summary.tsx     # 파티 요약
│   │       │   ├── party-card.tsx       # 파티 카드
│   │       │   └── single-party.tsx     # 단일 파티
│   │       │
│   │       ├── video/              # 영상 관련
│   │       │   ├── youtube-embed.tsx     # 유튜브 임베드
│   │       │   └── searchable-select.tsx # 검색 가능한 선택
│   │       │
│   │       ├── student/            # 학생 관련
│   │       │   ├── student-image.tsx # 학생 이미지
│   │       │   └── star-rating.tsx   # 별점
│   │       │
│   │       └── calculator/         # 계산기 관련
│   │           ├── raid-score-calculator.tsx
│   │           └── tactical-challenge-calculator.tsx
│   │
│   ├── lib/                        # 유틸리티 라이브러리
│   │   ├── api.ts                  # API 호출 함수
│   │   ├── party-filters.ts        # 파티 필터링 로직
│   │   └── utils.ts                # 유틸리티 함수
│   │
│   ├── store/                      # 상태 관리 (Zustand)
│   │   └── useBAStore.ts           # 전역 스토어
│   │
│   ├── types/                      # TypeScript 타입 정의
│   │   ├── raid.ts
│   │   ├── video.ts
│   │   └── types.d.ts
│   │
│   ├── constants/                  # 상수
│   │   └── assault.ts
│   │
│   └── utils/                      # 유틸리티
│       ├── character.ts
│       └── analytics.ts
│
├── docs/                           # 프로젝트 문서
│   └── FOLDER_STRUCTURE.md         # 이 문서
│
├── PLAN.md                         # 리팩토링 계획
├── CLAUDE.md                       # Claude 작업 가이드
└── package.json
```

---

## 📁 폴더별 상세 설명

### 1. `src/app/` - Next.js App Router

**용도**: 라우팅 및 페이지 정의

**규칙**:
- 파일 기반 라우팅 (파일명이 URL이 됨)
- `page.tsx` = 페이지 컴포넌트
- `layout.tsx` = 레이아웃 컴포넌트
- `_components/` = Private folder (라우팅에서 제외, 해당 경로에서만 사용)

**예시**:
```
app/video-analysis/page.tsx        → /video-analysis
app/video-analysis/[id]/page.tsx   → /video-analysis/123
app/video-analysis/_components/    → 라우팅 X, video-analysis에서만 사용
```

---

### 2. `src/components/` - 전역 공유 컴포넌트

**용도**: 앱 전체에서 재사용되는 컴포넌트

#### 2.1 `ui/` - 기본 UI 컴포넌트
- **shadcn/ui** 컴포넌트
- 버튼, 인풋, 카드 등 기본 빌딩 블록
- 비즈니스 로직 없음

#### 2.2 `layout/` - 레이아웃 컴포넌트
- 헤더, 푸터, 사이드바 등
- 앱 전체 레이아웃 구조

#### 2.3 `common/` - 공통 컴포넌트
- 로딩, 에러 페이지, 공지사항 등
- 비즈니스 로직이 적은 범용 컴포넌트

#### 2.4 `shared/` - 재사용 복합 컴포넌트
- Cascader, MultiSelect, Pagination 등
- 여러 곳에서 사용되는 복합 컴포넌트
- 약간의 로직 포함 가능

#### 2.5 `features/` - 도메인별 컴포넌트
- **핵심 비즈니스 도메인별로 그룹화**
- `raid/`: 레이드 관련 (파티 검색, 요약, 카드)
- `video/`: 영상 관련 (유튜브 임베드, 선택)
- `student/`: 학생 관련 (이미지, 별점)
- `calculator/`: 계산기 관련

**왜 `features/`인가?**
- 관련 컴포넌트를 한 곳에 모음
- 도메인 경계가 명확
- 확장성과 유지보수성 향상

---

### 3. `src/lib/` - 유틸리티 라이브러리

**용도**: 비즈니스 로직, API 호출

- `api.ts`: 서버 API 호출 함수
- `party-filters.ts`: 파티 필터링 로직
- `utils.ts`: 범용 유틸리티

---

### 4. `src/store/` - 상태 관리

**용도**: Zustand 전역 상태 관리

---

### 5. `data/` - JSON 데이터

**용도**: 정적 JSON 데이터 파일

**위치**: 프로젝트 루트 (src 외부)
- `src/` 내부에 있지 않음
- `import raidsData from "../../data/raids.json"` 형태로 import

---

## 🎯 네이밍 컨벤션

### 파일명
- **kebab-case**: `raid-search.tsx`, `party-card.tsx`
- Next.js 라우팅 시스템과 일치
- 크로스 플랫폼 호환성

### 컴포넌트명 (내부)
- **PascalCase**: `RaidSearch`, `PartyCard`
- React 컴포넌트 표준

### 함수명
- **camelCase**: `getFilters()`, `filteredPartys()`

---

## 📊 변경 이력

### 2025-01-04: 전체 재구성
- ❌ 제거: `atoms/`, `molecules/`, `custom/`
- ❌ 제거: `app/_components/main-page.tsx`
- ✅ 추가: `components/features/` (도메인별 그룹화)
- ✅ 통합: `page.tsx`에 로직 직접 작성
- ✅ 정리: Private folders (`_components/`) 명확화

**Before (Atomic Design)**:
```
components/
├── atoms/
├── molecules/
├── common/
└── custom/
```

**After (Features-based)**:
```
components/
├── ui/
├── layout/
├── common/
├── shared/
└── features/
    ├── raid/
    ├── video/
    ├── student/
    └── calculator/
```

---

## 🔍 컴포넌트 배치 가이드

### 새 컴포넌트를 어디에 둘까?

```
1. 한 페이지에서만 사용?
   → app/[route]/_components/

2. 여러 페이지에서 사용하는 기본 UI?
   → components/ui/ 또는 components/common/

3. 특정 도메인(레이드, 영상, 학생)과 관련?
   → components/features/[domain]/

4. 여러 곳에서 사용하는 복잡한 컴포넌트?
   → components/shared/
```

---

## ✅ 체크리스트

새 컴포넌트 추가 시:

- [ ] 파일명이 kebab-case인가?
- [ ] 컴포넌트명이 PascalCase인가?
- [ ] 올바른 폴더에 위치했는가?
- [ ] import 경로가 적절한가? (`@/` 절대경로 vs 상대경로)
- [ ] Private folder가 필요한가? (`_components/`)

---

## 📚 참고 자료

- [Next.js 15 App Router 공식 문서](https://nextjs.org/docs/app)
- [Next.js Project Structure](https://nextjs.org/docs/app/getting-started/project-structure)
- [Best Practices for Organizing Your Next.js 15 2025](https://dev.to/bajrayejoon/best-practices-for-organizing-your-nextjs-15-2025-53ji)
